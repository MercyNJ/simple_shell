#include "main.h"

/**
 * comments - Check if 1st character is a #.
 *
 * @lineptr: Pointer to character array of input.
 *
 * Description: Check for # character in a line of input.
 *
 * Return: Nothing
 */

void comments(char *lineptr)
{
	if (lineptr[0] == '#')
	{
		return;
	}
}
